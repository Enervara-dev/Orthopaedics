# Core config (chunking + graphrag shared)
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
truststore>=0.9

# Chunking pipeline
PyMuPDF>=1.24
pandas>=2.0

# LLM + vector + graph (graphrag retrieval + answer)
google-genai>=0.8
openai>=1.40
pinecone>=5.0
neo4j>=5.0
requests>=2.31

# Session memory (optional — falls back gracefully if absent)
redis>=5.0
orjson>=3.9

# HTTP service (app/main.py, deployed via render.yaml)
fastapi>=0.110
uvicorn[standard]>=0.29
